import { describe, it, expect } from "bun:test";
import { ColorWheelRoot } from "./ColorWheelRoot";

describe("ColorWheelRoot", () => {
  it("is defined", () => {
    expect(ColorWheelRoot).toBeDefined();
  });
});
