import { describe, it, expect } from "bun:test";
import { ColorWheelCheckerboard } from "./ColorWheelCheckerboard";

describe("ColorWheelCheckerboard", () => {
  it("is defined", () => {
    expect(ColorWheelCheckerboard).toBeDefined();
  });
});
