import { describe, expect, test } from "bun:test";
import { ColorSliderRange } from "./ColorSliderRange";

describe("ColorSliderRange", () => {
  test("is defined", () => {
    expect(ColorSliderRange).toBeDefined();
  });
});
