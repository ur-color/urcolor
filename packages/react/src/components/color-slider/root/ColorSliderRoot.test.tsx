import { describe, expect, test } from "bun:test";
import { ColorSliderRoot } from "./ColorSliderRoot";

describe("ColorSliderRoot", () => {
  test("is defined", () => {
    expect(ColorSliderRoot).toBeDefined();
  });
});
