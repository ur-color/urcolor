import { describe, expect, test } from "bun:test";
import { ColorSliderGradient } from "./ColorSliderGradient";

describe("ColorSliderGradient", () => {
  test("is defined", () => {
    expect(ColorSliderGradient).toBeDefined();
  });
});
