import { describe, expect, test } from "bun:test";
import { ColorSliderCheckerboard } from "./ColorSliderCheckerboard";

describe("ColorSliderCheckerboard", () => {
  test("is defined", () => {
    expect(ColorSliderCheckerboard).toBeDefined();
  });
});
