import { describe, expect, test } from "bun:test";
import { ColorSliderControl } from "./ColorSliderControl";

describe("ColorSliderControl", () => {
  test("is defined", () => {
    expect(ColorSliderControl).toBeDefined();
  });
});
