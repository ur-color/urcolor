import { ColorSwatch } from "./root/color-swatch";

export { ColorSwatch } from "./root/color-swatch";

/** Every `ColorSwatch` part, for a single entry in a component's `imports`. */
export const COLOR_SWATCH_DIRECTIVES = [ColorSwatch] as const;
